# Scanner Settings
SCAN_INTERVAL=30
MIN_LIQUIDITY=1000
MIN_PROFIT=0.02
PRICE_MIN=0.87
PRICE_MAX=0.96

# Strategy
STRATEGY=default
USE_WEBHOOK=false
LLM_ENABLED=false

# Redis (optional)
# REDIS_URL=redis://localhost:6379
CACHE_TTL=30

# Queue
QUEUE_MAX_SIZE=100
WORKER_COUNT=2

# Execution
MAX_RETRIES=3
RETRY_DELAY=2
GAS_LIMIT=300000
