# Polymarket Arbitrage Bot

> ⚡ 自动化套利交易机器人 - 10层架构

## 功能特性

- 🔍 **实时市场扫描** - 多策略市场筛选
- 📊 **套利检测** - 自动发现价差机会
- 🛡️ **风控引擎** - 仓位/回撤/亏损控制
- 📢 **监控告警** - Telegram 实时推送
- ⚙️ **交易队列** - 策略/执行解耦
- 💾 **缓存层** - Redis/内存缓存

## 架构

```
┌─────────────────────────────────────────┐
│ L1  入口层 (Runner)                     │
├─────────────────────────────────────────┤
│ L2  扫描器层 (Scanners)                 │
├─────────────────────────────────────────┤
│ L3  策略层 (Strategies)                 │
├─────────────────────────────────────────┤
│ L4  核心服务层 (Core Services)          │
├─────────────────────────────────────────┤
│ L5  风控层 (Risk Control)               │
├─────────────────────────────────────────┤
│ L6  监控层 (Monitoring)                │
├─────────────────────────────────────────┤
│ L7  执行层 (Execution)                 │
├─────────────────────────────────────────┤
│ L8  队列层 (Trade Queue)                │
├─────────────────────────────────────────┤
│ L9  缓存层 (Cache)                      │
├─────────────────────────────────────────┤
│ L10 配置层 (Config)                    │
└─────────────────────────────────────────┘
```

## 快速开始

### 1. 安装

```bash
git clone https://github.com/marcostrategy/polymarket-arbitrage-bot.git
cd polymarket-arbitrage-bot

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 2. 配置

```bash
# 复制配置
cp config/scanner.env.example config/scanner.env
cp config/wallet.env.example config/wallet.env

# 编辑配置
nano config/scanner.env
nano config/wallet.env
```

### 3. 运行

```bash
# 标准扫描
python runner.py

# 高级扫描 (策略)
python runner.py --advanced --strategy highprop

# 实时监听 (WebSocket)
python realtime_listener.py
```

## 配置说明

### scanner.env

```bash
# API
GAMMA_API=https://gamma-api.polymarket.com
CLOB_API=https://clob.polymarket.com
CLOB_KEY=your_api_key

# 扫描
SCAN_INTERVAL=30
MIN_LIQUIDITY=1000
MIN_PROFIT=0.02
PRICE_RANGE=0.87,0.96
```

### wallet.env

```bash
# 钱包
PRIVATE_KEY=0x...

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# 执行
MAX_RETRIES=3
GAS_LIMIT=300000
```

## 策略

| 策略 | 说明 |
|------|------|
| `default` | 标准扫描 |
| `highprop` | 高概率市场 (短期) |
| `whale` | 鲸鱼跟随 |

## 性能

| 指标 | 时间 |
|------|------|
| API 获取 | ~55ms |
| 价格获取 | ~30ms |
| 策略计算 | ~10ms |
| 链上确认 | ~3s |
| **总时间** | **~3.6s** |

vs 手动交易: **4x 更快**

## 文件结构

```
polymarket-arbitrage-bot/
├── runner.py              # 入口
├── scanner_v2.py         # 标准扫描
├── advanced_scanner.py   # 高级扫描
├── realtime_listener.py  # WebSocket 监听
├── execution_engine.py   # 交易执行
├── risk_engine.py        # 风控
├── monitor.py            # 监控
├── trade_queue.py        # 交易队列
├── cache_layer.py        # 缓存
├── config.py             # 配置
├── config/
│   ├── scanner.env
│   └── wallet.env
└── requirements.txt
```

## 注意事项

⚠️ 交易有风险，请谨慎使用

- 先在测试网验证
- 建议初始资金 $100-500
- 监控系统运行状态

## License

MIT
